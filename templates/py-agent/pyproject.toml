[project]
name = "py-agent"
version = "0.1.0"
description = "Starter: Claude API + Claude Agent SDK (copy this folder, then `uv sync`)"
requires-python = ">=3.12"
dependencies = [
    "anthropic>=1.7",
    "claude-agent-sdk>=0.2",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
